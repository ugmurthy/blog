<div class="col-lg-4 col-md-2">
  <div class="pt-3">
    Recent Blogs... 
  </div>
    {% for post in collections.posts | reverse %}
      <div class="bloglist" >
        <a href="{{ post.url | url }}" data-toggle="tooltip" title="Posted on: {{post.date | formatDate }}"> 
          <div class="text-info small line-height:1.1"> {{post.data.title}}</div>
        </a>
       
        {% if site.showkeywords %} 
          <div class="small font-weight-lighter"> &#9492 {{ post.data.keywords }} </div>
        {%else%}
          <div style="display:none" class="small"> {{ post.data.keywords }} </div>
        {%endif%}
    {# the following div is to ensure we search whole content #}  
    <div style="display:none">{{post.data.content}} </div>
    {% if site.showsummary %}
    <p class="text-muted small"> 
      {{ post.data.content | safe | summary( 80 ) }} 
      <a href="{{ post.url | url }}" data-toggle="tooltip" title="by: {{post.data.author}}"> 
            ...
      </a>
    </p>
    {% endif %}

    </div>
    {% endfor %}
</div>